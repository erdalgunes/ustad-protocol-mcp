# OpenAI API Configuration
OPENAI_API_KEY=your-api-key-here

# Model Configuration (gpt-3.5-turbo is cheapest)
OPENAI_MODEL=gpt-3.5-turbo

# Optional: Max tokens per perspective (lower = cheaper)
MAX_TOKENS_PER_PERSPECTIVE=500

# Optional: Temperature for variety
TEMPERATURE=0.7
